name: vite_ma_pompe
description: Trouvez la station service proche de vous

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^2.2.0
  latlong2: ^0.8.1
  geolocator: ^9.0.1
  permission_handler: ^10.0.0
  flutter_map_location_marker: any
  appwrite: ^6.0.0
  json_annotation: ^4.6.0
  maps_launcher: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner:
  json_serializable:

flutter:
  uses-material-design: true